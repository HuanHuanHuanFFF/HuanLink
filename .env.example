# Only secrets explicitly referenced by .huanlink/config/*.json belong here.
# URLs, IDs, models, policies, and other settings live in the JSON config tree.
HUANLINK_ONEBOT_ACCESS_TOKEN=
DEEPSEEK_API_KEY=
